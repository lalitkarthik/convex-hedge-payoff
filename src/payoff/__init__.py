"""The payoff engine.

Five modules, as #23 lays them out: `models` (the shared types - the seam), `pricing`
(the Black-76 core), `chain`, `strategy` and `presets`. The HTTP surface sits outside
those five.
"""
